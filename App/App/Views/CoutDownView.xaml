<UserControl x:Class="App.Views.CountDownView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:App.Views"
             mc:Ignorable="d" 
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="20"/>
        </Grid.RowDefinitions>
        <Viewbox Grid.Row="0">
            <TextBlock  HorizontalAlignment="Center" VerticalAlignment="Center" Text="{Binding CountText}" FontFamily="Consolas" TextDecorations="{x:Null}" FontWeight="Medium" Foreground="#FFE05656"/>
        </Viewbox>
        <Grid Grid.Row="1" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="Play" Width="120" Command="{Binding PlayCommand}"></Button>
            <Button Grid.Column="1" Content="Pause" Width="120"  Command="{Binding PauseCommand}"></Button>
            <Button Grid.Column="2" Content="Reset" Width="120"  Command="{Binding ResetCommand}"></Button>
        </Grid>
    </Grid>
</UserControl>
